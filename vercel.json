{
  "buildCommand": "cd admin && rm -rf node_modules && npm ci && npm run build",
  "outputDirectory": "admin/dist",
  "installCommand": "cd backend && npm install",
  "github": {
    "silent": true
  }
}